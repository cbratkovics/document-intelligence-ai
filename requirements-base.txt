# Core runtime: enough for the offline (lexical-only) path and the API.
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0
httpx==0.25.2

# Extraction and indexes
pypdf==3.17.0
numpy==1.24.3
chromadb==0.4.22

# Optional metrics endpoint (the app runs without it)
prometheus-client==0.19.0
