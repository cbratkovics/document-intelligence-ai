# Optional providers. Install on top of requirements-base.txt.
-r requirements-base.txt

# OpenAI embeddings / generation / LLM reranking (paid; needs OPENAI_API_KEY)
openai==1.6.1

# Local embeddings and the transformer cross-encoder reranker
# (models must be prepared with scripts/setup/init_models.py; no download at runtime)
sentence-transformers==2.2.2
torch>=2.0,<2.3
transformers>=4.30,<4.40

# Experimental components under app/ (not wired into the API)
redis==5.0.1
celery==5.3.4
scikit-learn>=1.3,<1.6
nltk>=3.8,<3.9
